# UI刷新 ローカル提出

**判定: 確認済み範囲のレビュー候補。公開元ソースブランチへの反映対象であり、GitHub Pagesの公開完了版や依頼全体の全操作検証完了版ではない。**

作業正本: `/Users/pc141/Documents/RF Basic Link Calculator/.ui-renewal-20260913/app`

基底: `9aab9eb5635afd08327b2069542746f3a4344562`（公開元）。基底との差は追跡53ファイル＋新規E2E 1ファイルで、提出manifestの全ハッシュと一致。元チェックアウトの切替、reset、stash、commitは実施せず、元の未コミット・未追跡23ファイルは最終SHA256照合で変更0件。`src/lib/rf/**` の変更も0件。ユーザーの追加指示により、この検証済み差分と提出記録を独立cloneから `feature/reach-distance-ui` へコミット・pushする。Pagesデプロイは別操作として実施しない。

## 起動

```sh
cd '/Users/pc141/Documents/RF Basic Link Calculator/.ui-renewal-20260913/app'
GITHUB_PAGES=true npm run build
npm run preview
```

確認用URL: `http://127.0.0.1:4173/rf-basic-link-calculator/`

2026-09-14 01:49 JST時点で自己所有の静的preview PID 19302が同URLをHTTP 200で提供中。ポート使用中は二重起動しない。

## 確認済み範囲のレビュー候補

- トップ: 5つの仕事から推奨ツールへ直接進める。全64ツールの検索・カテゴリ絞込み・件数・0件・URL状態・戻る操作を保持。
- Header/Footer: 重複していた長い一覧を整理し、ホーム・検索・全ツール・相談へ集約。比較ツールも検索レジストリへ追加。
- 共通入力: 編集途中の空欄や不正文字を勝手に0へ変えず、未算出状態と説明例への復帰を表示。共通結果は「分かったこと・条件・次の確認」を近接表示。
- 簡易リンク設計、FSPL、VSWR、同軸ケーブル損失、雑音電力: 個別UIを共通規則へ合わせた。簡易リンクは周波数・距離・送信電力・利得・受信感度・追加損失を項目別に検証し、既知の0と無効値を区別。1 km→1000 mでも+31.3 dBを維持した。FSPLの1 km→1000 m、雑音電力の125 kHz→125000 Hzでも物理条件と結果が変わらないことをChromeで実測。
- 単位換算の横断修正: 電界強度10 m→0.01 km、遠方界寸法100 mm→0.1 m、電池の動作間隔1時間→60分、総合リンク設計の実測アンカー1234.5 m→1.2345 km、GL以下NCUの地上距離300 m→0.3 kmで、表示値だけを換算して結果を保持するよう統一。Chromeで全5経路の結果不変を確認し、電池寿命は390px幅でも操作した。詳細は `logs/unit-conversion-browser-final.json`。
- 電池寿命: 容量、送受信電流・時間、動作間隔、スリープ電流、有効容量、温度、経年を項目別に検証。容量空欄と、送受信時間が周期を超える条件を該当欄で説明し、結果を未算出へ切り替える。標準値への復帰もChromeで確認した。
- 結果の読み方: 電界強度、遠方界距離、電池寿命の主結果へ、成立条件と次に確認するツールを近接表示。PagesのbasePath付きリンクで電池寿命→LoRa、電界強度→遠方界、遠方界→電界強度の遷移をChrome確認。遠方界は390×844でも結果文とリンクの折返しを目視確認した。詳細は `logs/battery-validation-result-guidance-browser-final.json`。
- 独立レビュー反映: GPT-5.6-solが、電池エキスパートの範囲外でも結果が残る点、電界強度・遠方界の空欄で旧結果が残る点、受信条件の無効理由がない点を特定。温度61℃、EIRP空欄、受信周波数0MHzをPages版で再現し、項目エラー・未算出・次リンク非表示まで修正確認した。電池からの次導線は「LoRaの場合」と用途を限定した。
- 共通ファーストビュー: 説明を削らず、基本ツール共通シェルの上余白と説明幅を調整。全基本ツールで計算領域の開始が400px以内、主結果下端が900px以内に入るE2Eを完走した。
- 重点境界値: FSPL、雑音電力、VSWR、同軸ケーブル損失で空欄・0・負数・不正文字を代表確認。同軸は周波数の不正時に本数まで誤表示していたエラーを項目別へ修正し、1.5本を拒否、10 GHzでは計算値と外挿警告を併記することをChromeで確認。詳細は `logs/priority-boundaries-final.json`。
- 総合リンク設計: 重複した導入を削り、結果詳細を初期折りたたみにした。STEP 1〜3で条件を調整し、STEP 4の1操作で結果を開いて要約へフォーカスできる。390pxで横はみ出し0。詳細・クイック双方の距離換算精度を小数3桁固定から有効数字15桁へ改め、実測アンカーのm/km切替も同じ物理距離を保持する。
- ケーブル・アンテナ位置比較: 条件入力・結果・保存の手順ナビ、条件へ戻る導線を追加。実測差 `-80→-74=+6 dB`、同値0、悪化-6、変更後0 dBmで+80を確認。不正文字・空欄は結果保留。相談用まとめでも+6 dBを保持し、ケーブル損失を二重控除しない。ブラウザー内保存後に入力を変え、復元で長さ1 m・結果+1.2 dBへ戻ることをChromeで確認。置換確認を画面内ダイアログへ変更し、Tab循環、Escape、元ボタンへのフォーカス復帰も実測した。schemaVersion 2のJSON書出し、不正JSONの拒否、正常JSONの貼付・確認付き復元に加え、書き出したJSONをOSファイル選択から読み戻す経路もPages版で確認した。
- 周波数用途地図: 公開版にもあったReact #418を開発版の完全な警告から特定。SVG `title` を単一文字列にして帯域グループへ移し、`aria-label`を保持。開発版とPages basePath静的版の新規Chromeタブでconsole error/warning 0件。
- 全64画面: 390pxで見出し・ページ幅を巡回。検出した3件の横はみ出し（周波数・波長、dB体感、セルラーバンド地図）を修正し、最終幅390pxを確認。各画面の扱いは `COVERAGE.md`。
- アクセシビリティ: axe-coreのWCAG 2.1 A/AA・serious/critical走査で初回11画面の色コントラスト違反を検出。共通結果ラベル・単位・補足と、未選択チップ・進捗・バッジを濃くし、デスクトップのホーム＋64ツールを65/65件で再合格した。全面的なWCAG準拠宣言ではない。

## 自動検証

最終ソースで次を成功確認。ログは `docs/ui-renewal/continuity/`。

- `npm run lint`: 成功
- `npx tsc --noEmit`: 成功
- `npm test`: 80ファイル、768テスト成功
- `GITHUB_PAGES=true npm run build`: 成功、basePath付き静的export生成
- 全64ツールの静的ディープリンク: HTTP 200
- `npx playwright test --list`: 成功、3ファイル125件（刷新13件、比較9件）を収集。
- `npm run test:e2e -- --workers=4`: 成功、3ファイル125件すべて合格（1.3分）。通常ツール、比較9件、刷新13件を同一走行で完走。
- `npx playwright test --config docs/ui-renewal/audit/accessibility.config.mjs`: 成功、デスクトップ65/65ページ合格（1.3分、WCAG 2.1 A/AAのserious/critical）。

Pages専用の `npm run test:comparison:e2e` は初回と許可された再試行の両方で9件を選択したが、この別設定では各テスト本体0msのChromium起動時にMachPort permission denied (1100)となった。同じ比較9件は上の全E2Eで合格し、Pages版は全64ルートHTTP 200とChrome実操作で別途確認した。静的basePathに対する自動比較9件の合格とは扱わない。追加再試行、テストの削除・skip、権限回避はしていない。詳細は `logs/playwright-final-run.json`。

## 未達の候補

- 比較の削除とOS印刷完了。JSON書出し、貼付経由とOSファイル選択経由の読込はChrome実操作で確認済み。削除は今回の確認用データを残したため未実施。印刷対象本文は画面で確認したが、印刷ボタン後はブラウザ制御が停止するためネイティブ画面の目視と印刷完了を確認していない。
- 64ツールすべての正常・空欄・0・負数・不正値・単位変更・適用外と、全機能のキーボード完遂。全画面は表示確認であり、個別機能合格ではない。
- Pages専用比較E2EをChromiumが安定起動できる環境で完走し、静的basePathに対する9件の自動回帰を確認すること。
- 390×844の全65ページaxe走査。初回と静的previewへ切り替えた再試行はいずれもテスト本体0msのMachPort拒否で、合否を判定できていない。
- 共通NumberInputの全利用箇所。重点画面の空欄・不正値と、ソース上で確認できた全単位セレクトの換算は実測したが、全利用箇所の操作回帰は未完了。

## 未達

- GitHub Pages公開。今回の追加指示はcommit・pushまでで、Pagesデプロイは実施しない。
- GPT停止を自動検出して03:00まで無人継続する経路。1操作のローカル検証・排他・提出生成は実測済みだが、自動予約やOS停止耐性はない。
- 全64機能の商用品質保証。確認済みの重大RF数値不一致や主要操作不能は残っていないが、未検証範囲を問題なしとは扱わない。

## 分担記録

- GPT-5.6-sol: 専用コピーでトップ・検索・Header/Footer・レジストリ・関連テストを実装。入力、差分、46.072秒の観測時間、採否は `REVIEWS.md` と `logs/gpt56-wave1.json`。
- Gemini `gemini-3.8-flash-high`: 現行導線への独立反証。16.623秒。実画面操作なし。根拠のないRFプリセット提案は不採用。
- Qwen3.6 35B: 画面仕様批評、50.295秒。Gemma4 12B: 営業視点の文案、53.548秒。qwen3-coder 30B: 境界レビュー12.402秒、検索レビュー11.860秒、最終差分レビュー74.475秒。JSON貼付レビューは初回180.005秒でtimeout、縮小再試行4.360秒で完了し、4指摘を根拠不一致として不採用。
- qwen3-coder 30Bの色差分レビュー: 初回17ファイルは325038 bytesでモデル起動前に入力上限拒否。9ファイルへ縮小したID `20260914-014601-ollama`は103.648秒、変更0件、具体的な競合指摘なし。ブラウザ・axe未実行のため、担当によるWCAG合格主張は不採用。
- 最終Qwenレビューの「Playwright・実機確認済み」という根拠のない記述は不採用。画面確認済みとする根拠はGPT司令塔が実施したChrome操作だけ。

## 利用枠と再開

週間55%はユーザー申告の開始基準で、現在値ではない。現在の週間残量、5時間残量、双方のリセット時刻は取得不能のため不明。推測していない。GPTモデル切替による回避なし。追加クラウド範囲拡大は行わず、最終レビューはローカルQwenで実施。

GPTを使わない1操作の再検証:

```sh
python3 "/Users/pc141/Documents/RF Basic Link Calculator/.ui-renewal-20260913/control/continue-local.py"
```

この処理は排他ロックを取り、lint、型、Vitest、Pages build、ローカル提出記録を順番に実行する。自動予約ではない。後任は `HANDOFF.md`、`WORKLOG.md`、`REVIEWS.md`、`logs/submission-manifest.json`、実ファイル、稼働プロセスを照合して再開する。
